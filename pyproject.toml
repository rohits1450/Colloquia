[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "tanglish-rag-dpo"
version = "0.1.0"
description = "RAG + DPO pipeline for English to Tanglish translation"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
include = ["src*"]
